<div class="min-h-screen bg-gradient-to-br from-amber-50 to-orange-50">
  <!-- ヘッダー -->
  <header class="bg-white shadow-sm border-b border-amber-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <h1 class="text-2xl font-bold text-amber-800">
          <%= link_to "📚 本色日記" ,root_path %>
        </h1>
        <%= link_to "新しい本を追加", new_book_path, 
            class: "bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 rounded-lg font-medium transition-colors" %>
      </div>
    </div>
  </header>

  <!-- メインコンテンツ -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- 書籍グリッド -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
      <% @books.each do |book| %>
        <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300 overflow-hidden">
          <!-- カラープレビュー -->
          <div class="h-4" style="background-color: <%= book.color_code %>"></div>
          
          <!-- 書籍情報 -->
          <div class="p-6">
            <h3 class="font-bold text-lg text-gray-800 mb-2 line-clamp-2">
              <%= book.title %>
            </h3>
          
            
            <!-- 感じた色の表示 -->
            <div class="flex items-center gap-3 mb-4">
              <span class="text-sm text-gray-500">感じた色:</span>
              <div class="flex items-center gap-2">
                <div class="w-8 h-8 rounded-full border-2 border-gray-200 shadow-inner" 
                     style="background-color: <%= book.color_code %>"></div>
                <span class="text-xs text-gray-600 font-mono">
                  <%= book.color_code %>
                </span>
              </div>
            </div>
            
            <!-- アクションボタン -->
            <div class="flex gap-2">
              <%= link_to "詳細", book_path(book), 
                  class: "flex-1 bg-amber-100 hover:bg-amber-200 text-amber-800 text-center py-2 px-3 rounded-lg text-sm font-medium transition-colors" %>
              <%= link_to "編集", edit_book_path(book), 
                  class: "flex-1 bg-blue-100 hover:bg-blue-200 text-blue-800 text-center py-2 px-3 rounded-lg text-sm font-medium transition-colors" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </main>
</div>
